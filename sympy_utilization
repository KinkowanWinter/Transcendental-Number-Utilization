# This is an example of SymPy
from sympy import *
sympy.init_printing(use_unicode=False, wrap_line=True)
x = symbol("x")
y = symbol("y")
f, g = symbols("f g", cls=Function)
print(expand((x+y)**6))
print(simplify((sin(x))/(cos(x))))
def function pi_multiples:
	for i in range(1, 100):
		print(pi * i)
